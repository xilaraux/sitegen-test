{
    "name": "sitegen-main",
    "version": "0.1.0",
    "dependencies": {
        "react": "*",
        "react-dom": "*"
    },
    "scripts": {
        "build:dev": "echo 'build done'",
        "coverage": "jest --coverage",
        "test": "jest --ci --testResultsProcessor=\"jest-junit\""
    },
    "devDependencies": {
        "babel-jest": "*",
        "babel-preset-env": "*",
        "babel-preset-react": "*",
        "jest": "*",
        "jest-junit": "^5.1.0"
    }
}
